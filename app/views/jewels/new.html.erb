  <div class="columns container forms">
    <div class="column is-full field ">
      <%= form_for(@jewels, url: '/jewels') do |f| %>
        <%= f.label :name, :class => 'label'  %>
        <%= f.text_field :name, :class => 'input' %>

        <%= f.label :desc, "Description", :class => 'label' %>
        <%= f.text_field :desc, :class => 'input' %>

        <%= f.label :tags, "Tags", :class => 'label' %>
        <%= f.text_field :tags, :class => 'input' %>

        <%= f.submit "Add Gem", class: "button is-primary" %>
      <% end %>
     <%= link_to "Back", to="/jewels", class: "button is-primary are-large column is-full" %>
    </div>
  </div>
